import React from "react";
import ReactDOM from "react-dom/client";
import FaceCapturePage from "./FaceCapturePage";

ReactDOM.createRoot(document.getElementById("root") as HTMLElement).render(
  <React.StrictMode>
    <FaceCapturePage />
  </React.StrictMode>,
);
